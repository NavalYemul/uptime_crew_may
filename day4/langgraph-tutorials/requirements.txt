# Use the root pyproject.toml for a single unified venv.
#
# Setup:
#   cd day4-agents-langgraph-mcp
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -e ".[all]"
#
# This installs all dependencies for BOTH src/day4/ AND langgraph-tutorials/
